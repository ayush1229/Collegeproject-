/**
 * api/phase.api.js — Phase and allocation state REST calls (TanStack Query API layer)
 */
export {
    getAllocationState,
    getCurrentBatch,
    triggerDevPhase,
    triggerResetPhase,
    getBatches,
} from './allocation.api.js';
